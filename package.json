{
  "name": "aumtech-production",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "build": "echo 'Python Production Environment: Skipping Node Build' && mkdir -p public && cp index.html public/"
  }
}
