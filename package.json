{
  "name": "aumtech-production",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "build": "cd 3_code/frontend && npm install && npm run build",
    "vercel-build": "cd 3_code/frontend && npm install && npm run build"
  }
}
